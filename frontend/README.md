# Frontend

Vite + React + TypeScript + Ant Design shell for MVP Push Gateway Step 1.

```bash
npm install
../scripts/dev-frontend.sh
../scripts/test-frontend.sh
```

The initial screen is an empty management console shell and does not call real APIs.
